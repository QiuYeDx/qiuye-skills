interface:
  display_name: "Geometric Mask Transition"
  short_description: "Build reliable geometric reveal transitions"
  default_prompt: "Use $geometric-mask-transition to choose the correct layer architecture, implement the reveal, and validate every transition state."
