# {{MODULE_TITLE}} 设计文档

| 项目 | 内容 |
| --- | --- |
| 需求文档 | ./requirements.md |
| 公共契约 | {{COMMON_REF}} |
| 状态 | 草稿 / 已确认（CP2） |
| 创建日期 | {{DATE}} |
| 更新日期 | {{DATE}} |

设计门要求见 Skill 的 `references/quality-gates.md`；前端与后端各节的产出要求见 `references/frontend-quality.md`、`references/backend-quality.md`。不涉及的章节写「不涉及」并说明原因，不要删除。

## 1. 设计目标与原则

-

## 2. 现状与约束

通过读代码得出，写具体路径。

| 类型 | 路径或名称 | 现状 | 对本设计的影响 |
| --- | --- | --- | --- |
| 分层与命名约定 |  |  |  |
| 组件库 / 设计 token |  |  |  |
| 接口风格 / 响应结构 |  |  |  |
| 鉴权方式 |  |  |  |
| 相关已有实现 |  |  |  |
| 已知坑 |  |  |  |

## 3. 代码落点

源码目录遵循项目规范；本表是范围栅栏，开发会话只改这里列出的路径及其测试。

| 层 | 路径 | 新建/修改 | 职责 |
| --- | --- | --- | --- |
| 前端页面 |  |  |  |
| 前端组件 |  |  |  |
| 前端状态/接口层 |  |  |  |
| 后端接口层 |  |  |  |
| 后端服务层 |  |  |  |
| 后端数据层 |  |  |  |
| 迁移 |  |  |  |
| 测试 |  |  |  |

## 4. 数据模型

### 4.1 实体与关系

| 实体 | 字段 | 类型 | 必填 | 默认 | 约束/索引 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

关系：

### 4.2 表结构与迁移

- 表名：
- 迁移脚本位置：
- 回滚方式：
- 软删除策略：

## 5. 状态机

| 实体 | 状态 | 可迁移到 | 触发条件 | 触发者 | 非法迁移错误码 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

不可逆点：

## 6. 接口契约

统一响应结构、错误码命名、分页参数以公共契约为准。每个接口一节。

### 6.1 <接口名>（关联 R-{{MOD}}-xx）

- 方法与路径:
- 鉴权与权限点:
- 幂等性:
- 请求参数:

  | 参数 | 位置 | 类型 | 必填 | 范围/枚举 | 默认 | 说明 |
  | --- | --- | --- | --- | --- | --- | --- |
  |  |  |  |  |  |  |  |

- 请求示例:

  ```json
  {}
  ```

- 响应示例:

  ```json
  {}
  ```

- 错误码:

  | 错误码 | 触发条件 | 前端展示 |
  | --- | --- | --- |
  |  |  |  |

- 分页/排序/筛选:

## 7. 后端设计

- 分层与新增类/文件职责:
- 事务边界:
- 并发与幂等:
- 校验规则（参数 / 业务）:
- 错误处理与日志:
- 性能边界（数据规模、分页上限、批量上限、缓存）:
- 外部依赖（超时 / 重试 / 降级）:
- 安全（越权检查、脱敏、审计）:

## 8. 前端设计

### 8.1 页面/视图清单

| 页面 | 路由 | 入口 | 权限 | 关联需求 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 8.2 信息架构与布局

每页的区域划分、主操作位置、信息层级。可用 ASCII 线框。

```text
+--------------------------------------+
| 标题                    [主操作按钮] |
+--------------------------------------+
| 筛选区                               |
+--------------------------------------+
| 列表/内容区                          |
+--------------------------------------+
```

### 8.3 组件树与复用

| 组件 | 来源（项目组件库 / 新建） | 职责 | props 要点 |
| --- | --- | --- | --- |
|  |  |  |  |

### 8.4 状态设计（五态）

| 页面/区块 | loading | empty | error | success | partial/disabled |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

表单额外：校验中 / 提交中 / 提交失败 的表现。

### 8.5 交互细节

| 元素 | 触发 | 行为 | 反馈 | 异常 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

表单校验规则（与接口契约一致）：

| 字段 | 规则 | 错误文案 | 校验时机 |
| --- | --- | --- | --- |
|  |  |  |  |

破坏性操作确认文案与后果说明：

### 8.6 数据流

- 数据来源接口:
- 状态存放位置（局部 / 全局 / URL）:
- 缓存与刷新策略:
- 乐观更新:
- 并发编辑处理:

### 8.7 响应式

| 断点 | 布局变化 |
| --- | --- |
|  |  |

### 8.8 文案清单

| 位置 | 文案 |
| --- | --- |
| 页面标题 |  |
| 主按钮 |  |
| 空态 |  |
| 错误 |  |
| 确认框 |  |
| 成功提示 |  |

### 8.9 可访问性与动效

- 焦点顺序与可见焦点:
- aria-label:
- 动效（目的 / 时长 / reduced-motion）:

### 8.10 视觉基准

- 设计 token / 主题:
- 密度、圆角、阴影层级:
- 强调色使用规则:

## 9. 错误处理与提示映射

| 后端错误码 | 前端展示方式 | 文案 | 用户可做什么 |
| --- | --- | --- | --- |
|  |  |  |  |

## 10. 需求追踪矩阵

每条 R 至少对应一个设计元素；任务列在 tasks.md 后回填。

| 需求 | 设计元素（接口 / 页面 / 表 / 组件） | 任务 |
| --- | --- | --- |
| R-{{MOD}}-01 |  |  |

## 11. 测试策略

| 层 | 覆盖内容 | 工具/命令 |
| --- | --- | --- |
| 单测 |  |  |
| 接口测试 |  |  |
| 组件测试 |  |  |
| 端到端 |  |  |

## 12. 风险与决策记录

| 编号 | 决策 | 备选方案 | 放弃原因 | 风险 |
| --- | --- | --- | --- | --- |
| D-01 |  |  |  |  |
