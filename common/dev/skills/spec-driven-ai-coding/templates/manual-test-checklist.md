# {{TITLE}} 批次验收

日期：{{DATE}}
批次：
集成版本/工作树快照：
环境与测试数据：
执行人：

## 用户场景与端到端验证

| 场景 / AC | 前置条件与步骤 | 预期 | 实际与证据 | 结果 |
| --- | --- | --- | --- | --- |

## 适用的专项检查

按变更选择：浏览器状态、权限/匿名访问、键盘焦点、断点、兼容性、真实服务、迁移/恢复。
不适用写理由；环境缺失写未执行，不能勾通过。测试只使用可控数据，不清理真实业务数据。

## 集成检查与风险

所有模块已完成不等于集成后可用。写明合并基线、端到端命令/步骤、结果、回退方式。

## 验收结论

通过 / 待验证 / 不通过；确认人、来源、批准范围；反馈按变更类型处理。
