# 示例索引

[m-filter-export](m-filter-export/README.md)：完整 M 格式、Python 纯函数、实际单测日志、任务完成记录；没有虚构用户验收或远程合并。
现实中同样小函数可用 S；本例采用 M 是为了演示文件之间如何衔接。

[l-rolling](l-rolling/README.md)：只有当前 I1 被细化，I2 是 deferred BR；无强制 common。只做规划结构验证，没有业务实现或浏览器运行。
其 ready 检查通过，但实施前批准检查应失败，演示“有文档不等于获准写代码”。

[scenarios](scenarios.md)：S、缺陷修复、技术实验、浏览器不可用、并行冲突与连续任务的行为示例。
这些是预期执行方式，不是已经运行过的 Agent 对话成绩。
